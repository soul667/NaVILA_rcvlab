# TODO List
你需要做的事情

## 真机部署环境以及代码
我们最后要在真实机器人上面部署，所以我希望你可以给我一套docker compsoe的环境以及构建docker镜像，但是在本地构建docker镜像十分的慢，所以构建镜像你应该是使用github action进行构建并且放到github的packages上面，这样我们就可以直接从github packages上面拉取镜像了。

目前我的打算就是 你从 ros2 的官方镜像开始from，然后配置环境和以来，然后通过ros2节点向外发消息，同时为不同的机器人写不同的包，比如对于逐际动力 来说，他是一个包，负责将外部的消息转换为标准的消息格式，比如外面我的相机节点以及发送消息的节点不一样会，但是我只下网我有一套标准的消息格式，在我的代码里曼，然后通过这个不同机器人的转换节点来将外部的消息转换为标准的消息格式，这样我们就可以在不同的机器人上面使用同样的代码了。

然后通过docker copose 可以再部署一些网页面板，来辅助进行可视化和调试

